# Adopt K3 max for the engine

The CTO selected K3 after reviewing the recorded comparison. Ship the tested
max-effort configuration through personal OpenRouter keys and MrCall credits.
This is a product decision under limited evidence, not a claim of model parity.

Every productive K3 request must carry its reasoning controls and combined
8192-token output ceiling before quotation and budget reservation. Translate
text and function histories, retain actual cost receipts, reject unsupported
inputs before dispatch, and never fall back to another paid model automatically.
Remove hidden Anthropic defaults from auxiliary engine calls. Preserve personal
explicit configuration, current daily limits and paused preparation queues.

Deployment requires the compatible credit proxy first and a pinned engine
release second. Production already uses MrCall credits; migration of the other
three 124 profiles awaits the CTO's billing preference. No provider key may be
copied from server secrets into a user profile. Current limits are USD20/day for
production and USD5/day for each of the other three profiles.
