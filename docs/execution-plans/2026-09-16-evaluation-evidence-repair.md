---
status: completed
---
# Evaluation evidence repair delivery

Brief: [reviewable evidence](../briefs/2026-09-16-evaluation-evidence-repair.md).

1. Show the CTO the actual remaining alleged errors: incoming message, relevant
   earlier/latest replies, model action and exact disputed assertion. Re-read
   whole threads, actively checking whether later messages change earlier advice.
2. Read company evidence without model calls or live writes. Record dated source
   excerpts and provenance, separating generated memory, sent correspondence and
   direct CTO instructions. Preserve uncertainty and historical cutoff boundaries.
3. Add an offline evidence module with complete per-label source sections,
   stable hashes, exact-quote validation and separately timestamped later facts.
   Hide model/provider/effort controls. Build from each cell's actual request and
   resolved clock, never the last variant stored under a shared case ID.
4. Validate separate task-decision, explanation-grounding, factual-truth and
   operational-impact dimensions. Require declared complete section coverage and
   source/output quotations. Do not claim mechanical validation proves truth.
5. Integrate bundle production into the grading preparer. Aggregate optional
   dimensions independently; legacy grades remain explicitly unassessed under
   the new protocol. Require actual parser acceptance for usable counts.
6. Add targeted offline regressions for absent context/quotes, hash mismatch,
   paired-input isolation, unknown payment truth with correct no-action, later
   evidence separation, missing legacy assessment and parser rejection.
7. Independently review code, source casecards and final reporting. Exercise the
   new path on recorded evidence without altering old packs or paying to rerun
   outputs. Publish the complete comparison with uncertain claims separated from
   proven losses, reconcile living docs, commit and push reviewed work.

## Recorded delivery evidence

- The preparer reproduces all 180 archived labels, masked outputs and identity
  mappings byte-equivalently as JSON values, adding complete evidence bundles
  in a separate private directory. Original packs are unchanged.
- Nine targeted version 2 reviews cover all three K3 arms on payment, Dominique
  and SCASO. Exact source/output quotations and full section coverage validate.
- A private 180-output queue links every historical allegation to complete
  captured context and output. It labels allegations as superseded assessments,
  not established errors.
- The complete selected comparison contains 60 cases and 240 outputs: three
  K3 arms plus historical Opus. All four semantic input hashes match per case.
  Post-hoc source review covers all three 20-case role/cohort partitions, with
  every adverse judgment peer-reviewed and explicit adjudications preserved.
  Model identities are disclosed and the data are already exposed.
  This cannot become a fresh holdout or certify production error rates.
- Offline code review and documentation review pass. No paid calls, production
  changes or outgoing correspondence are part of this repair.

- Final offline evaluation suite: 153 passing tests. Targeted Ruff and root
  documentation mechanical checks pass. Complete source review, exact-quotation
  binding checks and separate peer review cover the selected comparison. The
  [reviewed report](../evaluations/2026-09-16-reviewed-model-comparison.md) and
  private complete finding inventory distinguish policy violations, factual
  uncertainty and integration defects. Earlier full-study tables are preserved
  historical snapshots; GLM/unselected cases are not silently regraded.
